"""GraphQL surface — hand-written Strawberry types/resolvers over the same
`models.py` + `queries/*.py` the REST routers use. See `types.py` for why
this isn't derived from `schemas.py` via Strawberry's pydantic bridge.
"""
